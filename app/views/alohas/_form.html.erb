<%= form_for(@aloha) do |f| %>
  <% if @aloha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aloha.errors.count, "error") %> prohibited this aloha from being saved:</h2>

      <ul>
      <% @aloha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
