<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration %>
  </div>
  <div class="field">
    <%= f.label :filepath %><br>
    <%= f.file_field :filepath %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.select :album_id, options_from_collection_for_select(@albums, "id", "name") %>
  </div>
  <div class="field">
    <%= f.label :track_num %><br>
    <%= f.number_field :track_num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
